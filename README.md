# Nginx Proxy Wodby service

Standalone HTTP reverse proxy for Wodby 2.0 applications.

The service requires an HTTP backend and uses the Wodby Nginx `http-proxy`
preset. It has no build configuration and cannot be connected to a build
source or receive a custom build image.
